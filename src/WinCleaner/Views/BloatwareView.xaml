<UserControl x:Class="WinCleaner.Views.BloatwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <StackPanel>
    <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}"/>
    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}"/>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
      <Button Content="Refresh" Command="{Binding ReloadCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
      <Button Content="Remove Selected" Command="{Binding RemoveSelectedCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
      <Button Content="Uninstall OneDrive" Command="{Binding UninstallOneDriveCommand}" Style="{StaticResource DangerButtonStyle}"/>
    </StackPanel>

    <TextBlock Text="Loading..." Foreground="{StaticResource TextMutedBrush}"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

    <ItemsControl ItemsSource="{Binding Items}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <CheckBox IsChecked="{Binding IsSelected}" IsEnabled="{Binding CanSelect}" VerticalAlignment="Top" Margin="0,2,12,0"/>
              <StackPanel Grid.Column="1">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Title}" Style="{StaticResource CardTitleStyle}"/>
                  <Border Background="{Binding Risk, Converter={StaticResource RiskToBrush}}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                    <TextBlock Text="{Binding RiskLabel}" FontSize="10" Foreground="#111"/>
                  </Border>
                  <TextBlock Text="{Binding CriticalLabel}" Foreground="{StaticResource WarningBrush}" Margin="10,0,0,0" FontSize="11"/>
                </StackPanel>
                <TextBlock Text="{Binding PackageName}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"
                           FontSize="12" LineHeight="18"
                           ToolTip="{Binding Description}"/>
                <TextBlock FontSize="11" Margin="0,4,0,0">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                      <Setter Property="Text" Value="Not installed"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                          <Setter Property="Text" Value="Installed"/>
                          <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
  </ScrollViewer>
</UserControl>
