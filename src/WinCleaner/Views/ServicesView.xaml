<UserControl x:Class="WinCleaner.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <StackPanel>
    <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}"/>
    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}"/>

    <DockPanel Margin="0,0,0,12">
      <Button DockPanel.Dock="Right" Content="{DynamicResource common.disableAll}" Command="{Binding DisableCategoryCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
      <Button DockPanel.Dock="Right" Content="{DynamicResource common.refresh}" Command="{Binding ReloadCommand}" Style="{StaticResource SecondaryButtonStyle}"/>
      <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Width="200" HorizontalAlignment="Left"/>
    </DockPanel>

    <ItemsControl ItemsSource="{Binding Items}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <StackPanel>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Title}" Style="{StaticResource CardTitleStyle}"/>
                  <Border Background="{Binding Risk, Converter={StaticResource RiskToBrush}}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                    <TextBlock Text="{Binding RiskLabel}" FontSize="10" Foreground="#111"/>
                  </Border>
                </StackPanel>
                <TextBlock Text="{Binding ServiceName}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"
                           FontSize="12" LineHeight="18"
                           ToolTip="{Binding Description}"/>
                <TextBlock FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"
                           Text="{Binding StatusLine, Mode=OneWay}"/>
              </StackPanel>
              <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{DynamicResource svc.disable}" FontSize="10" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center"/>
                <ToggleButton Style="{StaticResource SwitchToggleStyle}"
                              IsChecked="{Binding IsDisabled, Mode=OneWay}"
                              IsEnabled="{Binding IsEnabled}"
                              Command="{Binding ToggleCommand}"/>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
  </ScrollViewer>
</UserControl>
