<UserControl x:Class="WinCleaner.Views.VisualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
  <StackPanel>
    <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleStyle}"/>
    <TextBlock Text="{Binding Subtitle}" Style="{StaticResource PageSubtitleStyle}"/>

    <ItemsControl ItemsSource="{Binding Items}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <StackPanel>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Title}" Style="{StaticResource CardTitleStyle}"/>
                  <Border Background="{Binding Risk, Converter={StaticResource RiskToBrush}}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0">
                    <TextBlock Text="{Binding RiskLabel}" FontSize="10" Foreground="#111"/>
                  </Border>
                </StackPanel>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0"
                           FontSize="12" LineHeight="18"
                           ToolTip="{Binding Description}"/>
              </StackPanel>
              <ToggleButton Grid.Column="1" Style="{StaticResource SwitchToggleStyle}"
                            IsChecked="{Binding IsApplied, Mode=OneWay}"
                            IsEnabled="{Binding IsEnabled}"
                            Command="{Binding ToggleCommand}"/>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <TextBlock Text="{Binding StartupTitle}" Style="{StaticResource SectionHeaderStyle}"/>
    <Button Content="Open Virtual Memory Settings" Command="{Binding OpenPagefileCommand}" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" Margin="0,0,0,12"/>

    <ItemsControl ItemsSource="{Binding StartupItems}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock Text="{Binding Name}" Style="{StaticResource CardTitleStyle}"/>
                <TextBlock Text="{Binding CommandText}" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                <TextBlock Text="{Binding Hive}" FontSize="10" Foreground="{StaticResource TextMutedBrush}"/>
              </StackPanel>
              <ToggleButton Grid.Column="1" Style="{StaticResource SwitchToggleStyle}"
                            IsChecked="{Binding IsEnabled, Mode=OneWay}"
                            Command="{Binding ToggleCommand}"/>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
  </ScrollViewer>
</UserControl>
